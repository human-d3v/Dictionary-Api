{
  "name": "fetchdef",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "commander": "^12.0.0"
  },
  "scripts": {
    "compile": "bun build src/cli.ts --compile --outfile=dict-api --target=bun"
  }
}